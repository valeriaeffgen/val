# Chaves da Val — copie para .env e preencha. Nunca comite o .env (segredos).

# Supabase (seção 9): auth, banco e caixa das cartas.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# API da Anthropic (seção 7): camada generativa, sempre com a Constituição no
# system prompt. ATENÇÃO: a chave da Anthropic NÃO deve ficar no frontend —
# ela vive no backend (ex.: função Supabase). Mantida aqui só como referência.
ANTHROPIC_API_KEY=
